import "server-only";

// Phase 8: compute corrected times and assign positions.
// Called by finishRace once all entries have a terminal status.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function computeRaceResults(_raceId: string): Promise<void> {}
